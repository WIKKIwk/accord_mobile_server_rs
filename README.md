# Accord Mobile Server RS

Rust port of `accord_mobile_server`.

This project is intentionally built in small sections. The first section is the auth/session foundation, not the whole Go server skeleton at once.
